You are editing a Vite + React + Tailwind site. In Navigation below, fix two issues:

Linktree logo gets squished at some widths

“Pride Mode” text is too close to the Linktree logo on certain screens

Make the header resilient at the lg breakpoint and above. Keep the design and existing test IDs.

What to change

Keep the logo at left as-is.

In the Desktop Navigation cluster (hidden lg:flex ...):

Split into 3 groups: Pride Mode (left), Nav Links (center, can wrap), Social Icons (right).

Give the Pride Mode group shrink-0 and responsive gap so its label never crowds neighboring items; add whitespace-nowrap on the label.

Put the Nav Links in a flex container with flex-1 min-w-0 flex-wrap so links wrap instead of compressing icons.

Put the Social Icons (Facebook, Instagram, Linktree) in a shrink-0 group with responsive gap.

On the Linktree <img>, add:

width/height attributes (reserve layout)

className="h-8 w-auto object-contain shrink-0" to prevent distortion

In the Mobile Sheet, add object-contain shrink-0 to the Linktree <img> as well.

Acceptance criteria

Linktree logo maintains aspect ratio and never compresses (no squish) from lg → 2xl.

Clear, consistent spacing between “Pride Mode” and the Social Icons cluster at all widths (no crowding).

If space is tight at lg, nav links wrap, but icons and Pride Mode do not shrink or overlap.

No visual regressions; all data-testid values unchanged.

Patch to apply (replace only the marked section)
// === replace ONLY the Desktop Navigation and Mobile Linktree <img> blocks ===

// Desktop Navigation
<div className="hidden lg:flex items-center w-full flex-nowrap gap-4">
  {/* Pride Mode: fixed-size cluster */}
  <div className="pride-toggle-switch-container flex items-center shrink-0 gap-x-2 sm:gap-x-3 md:gap-x-4">
    <span
      id="pride-toggle-label"
      className="font-semibold pride-text-animated whitespace-nowrap leading-none"
    >
      Pride Mode
    </span>
    <label className="pride-toggle-switch">
      <input
        type="checkbox"
        id="prideToggle"
        checked={isPrideMode}
        onChange={togglePrideMode}
        aria-labelledby="pride-toggle-label"
        aria-label="Pride Mode"
        data-testid="toggle-pride-mode"
      />
      <span className="pride-switch-track">
        <span className="pride-switch-thumb"></span>
      </span>
    </label>
  </div>

  {/* Nav Links: flexible, can wrap */}
  <div className="flex items-center flex-1 min-w-0 flex-wrap justify-center gap-1 sm:gap-2">
    {navLinks.map((link) => (
      <a
        key={link.label}
        href={link.href}
        target={link.isAnchor ? undefined : "_blank"}
        rel={link.isAnchor ? undefined : "noopener noreferrer"}
        className="nav-link text-foreground hover:text-muted-foreground px-3 py-2 rounded-md text-sm font-medium focus:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
        data-testid={link.testId}
      >
        {link.label}
      </a>
    ))}
  </div>

  {/* Social Icons: fixed-size cluster */}
  <div className="flex items-center shrink-0 gap-2 sm:gap-3 md:gap-4">
    <a
      href={links.facebook}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Follow us on Facebook"
      className="hover:opacity-80 transition duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 rounded-md"
      data-testid="link-facebook-desktop"
    >
      <FaFacebook className="h-8 w-8 text-foreground" />
    </a>
    <a
      href={links.instagram}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Follow us on Instagram"
      className="hover:opacity-80 transition duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 rounded-md"
      data-testid="link-instagram-desktop"
    >
      <FaInstagram className="h-8 w-8 text-foreground" />
    </a>
    <a
      href={links.social}
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Follow us on Social Media"
      className="hover:opacity-80 transition duration-200 focus:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 rounded-md"
      data-testid="link-social-desktop"
    >
      <img
        src="/linktree-logo.png"
        alt="Linktree"
        width={32}
        height={32}
        className="h-8 w-auto object-contain shrink-0 rounded-lg"
        decoding="async"
        loading="lazy"
      />
    </a>
  </div>
</div>

// Mobile Sheet — Social Links — Linktree <img> only
<img
  src="/linktree-logo.png"
  alt="Linktree"
  width={24}
  height={24}
  className="h-6 w-auto object-contain shrink-0 rounded-lg"
/>

Notes

shrink-0 on the icon cluster and image prevents flex shrink squish.

flex-1 min-w-0 flex-wrap on the links lets links adapt first, preserving the Pride Mode and icon spacing.

whitespace-nowrap on the “Pride Mode” label keeps it on one line so it doesn’t tuck under icons.